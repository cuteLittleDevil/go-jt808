---
description: 提交代码前必须先预览方案并等待用户确认
alwaysApply: true
---

# 提交前预览确认

在执行任何会把改动提交到版本库的操作之前（包括但不限于 `git add`、`git commit`、`git push`、创建/更新 PR），必须先停下来让用户预览并确认方案。

## 必须遵守

1. **先给方案，再动手提交**：完成实现或准备提交时，先用简洁中文说明本次拟提交内容，不要直接执行 commit / push。
2. **方案需可选择**：当存在多种合理做法时（例如拆成多个 commit、是否包含某文件、commit message 写法、是否开 PR），列出可选方案，并标明你推荐的一项；等用户明确选择后再继续。
3. **预览内容至少包含**：
   - 改动目的（一句话）
   - 涉及的主要文件 / 模块
   - 拟用的 commit message（可给 1–2 个备选）
   - 是否 push、是否创建/更新 PR
   - 若有风险或副作用，简要说明
4. **等待明确确认**：只有在用户明确回复同意（例如「确认」「按方案 A」「可以提交」）后，才执行 `git add` / `git commit` / `git push` / 创建 PR。
5. **用户改方案时以用户为准**：用户要求调整范围、message 或拆分方式时，按用户选择更新预览后再提交。

## 例外

- 用户在同一条消息里已经明确要求「直接提交 / 直接 commit / 直接 push」，且没有要求先看方案时，可按该指示执行，但仍应在提交前用一两句话说明将要提交什么。
- 纯只读操作（查看 status、diff、log）不受本规则限制。

## 反例

- 改完代码后未经确认就自动 `git commit` 并 `git push`
- 只说「我准备提交了」却不给出可确认的方案摘要
- 有多种方案时擅自选定并提交，未让用户选择
